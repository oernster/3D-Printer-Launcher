# Runtime dependencies: the five packages this project actually imports.
#
#   pip install -r requirements.txt
#
# Every entry below is imported by shipped code:
#
#   PySide6   the launcher UI (main.py, main_window.py, runner_widget.py,
#             tool_form.py, manage_tools_dialog.py)
#   Flask     the temperature web apps (VoronTemps/app.py, qidi-temps/app.py)
#   waitress  the WSGI server those two apps are served by
#   aiohttp   the Moonraker HTTP client (moonraker_client.py)
#   paramiko  the webcam restart helper (qidiwebcamdrestart/webcamdrestart.py)
#
# Transitive dependencies are left to pip. Pinning them here made this file a
# snapshot of one machine rather than a statement of what the project needs.
#
# The versions are floors rather than pins: each is the version the project was
# last verified against. Build tooling is not a runtime dependency and lives in
# requirements-dev.txt.

PySide6>=6.10.1
Flask>=3.1.0
waitress>=3.0.2
aiohttp>=3.11.13
paramiko>=3.5.1
